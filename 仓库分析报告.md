# deep-searcher 仓库分析报告

## 1. 仓库定位与目标

`deep-searcher` 是一个面向私有知识检索与问答/报告生成的 Python 项目，核心能力是把**离线数据加载+向量检索+LLM 推理**串成一条可配置的 RAG 流水线，支持 CLI 与 FastAPI 两种使用方式。

从代码与文档可以看出，该项目强调：
- 多 LLM、多 Embedding、多 VectorDB、多 Loader 的插件化切换
- 复杂问题的多轮检索与反思（DeepSearch / ChainOfRAG）
- 私有数据场景下的可落地能力（本地文件、网站爬取、集合路由）

---

## 2. 技术栈与工程形态

- **语言**：Python >= 3.10
- **包管理/环境**：uv（文档与 CI 均以 uv 为主）
- **核心依赖**：
  - LLM：openai、anthropic、google-genai、ollama、boto3、ibm-watsonx-ai 等
  - 向量库：pymilvus（默认）、qdrant-client、azure-search-documents、oracledb
  - 文档处理：langchain-text-splitters、pdfplumber、unstructured、docling
  - 服务接口：fastapi + uvicorn
- **代码规范**：ruff format + ruff check
- **测试**：pytest（按模块组织）
- **文档**：mkdocs + mkdocs-material
- **发布**：PyPI tag 发布工作流

---

## 3. 目录结构总览

### 3.1 核心目录
- `deepsearcher/`：主包（核心实现）
- `tests/`：单元测试与模块测试
- `docs/`：文档站内容
- `examples/`：示例脚本与示例数据
- `evaluation/`：评估脚本与配置
- `.github/workflows/`：CI/CD

### 3.2 deepsearcher 包内模块分层

- `configuration.py`
  - 配置加载（`config.yaml`）
  - 模块工厂（动态实例化 LLM/Embedding/Loader/VectorDB）
  - 全局单例初始化（`init_config`）
- `offline_loading.py`
  - 离线数据导入链路（本地文件、网站爬取）
- `online_query.py`
  - 查询入口（query/retrieve/naive）
- `agent/`
  - `DeepSearch`、`ChainOfRAG`、`NaiveRAG`、`RAGRouter`、`CollectionRouter`
- `llm/`、`embedding/`、`vector_db/`
  - 各 provider 实现与统一接口
- `loader/`
  - 文件加载器、网页爬虫、文本切分器
- `utils/log.py`
  - 日志与彩色输出

---

## 4. 关键运行链路分析

### 4.1 初始化链路

入口在 `deepsearcher.configuration.init_config(config)`：
1. 读取 `config.yaml` 的 provider 选择与参数
2. 通过 `ModuleFactory` 动态创建：
   - `llm`
   - `embedding_model`
   - `file_loader`
   - `web_crawler`
   - `vector_db`
3. 构建默认检索器：
   - `default_searcher = RAGRouter([DeepSearch, ChainOfRAG])`
   - `naive_rag = NaiveRAG(...)`

这使得项目可以在不改业务代码的情况下，通过配置切换 provider。

### 4.2 离线加载链路（数据入库）

### 本地文件：`load_from_local_files`
1. 归一化 collection 名称
2. `vector_db.init_collection(...)`
3. `file_loader.load_file/load_directory`
4. `split_docs_to_chunks`（chunk + wider_text 窗口）
5. `embedding_model.embed_chunks`
6. `vector_db.insert_data`

### 网站加载：`load_from_website`
流程类似，只是文档来源改为 `web_crawler.crawl_urls`。

### 4.3 在线查询链路

`online_query.query` 直接委托给 `configuration.default_searcher.query`。

默认是 `RAGRouter`，路由到以下 agent 之一：
- `DeepSearch`：偏报告生成与综合分析
- `ChainOfRAG`：偏多跳事实检索

### 4.4 DeepSearch 策略

核心特征：
- 子问题拆解（SUB_QUERY_PROMPT）
- 迭代检索 + rerank 过滤（YES/NO）
- 反思补全查询（REFLECT_PROMPT）
- 最终汇总回答（SUMMARY_PROMPT）
- 支持异步并发检索（`asyncio.gather`）

该策略更偏“广覆盖、可扩展”的深度搜索。

### 4.5 ChainOfRAG 策略

核心特征：
- 基于中间问答上下文逐轮生成 follow-up query
- 每轮检索后先生成中间答案
- 再选出“支持该答案”的证据文档
- 可选 early stopping
- 最终基于中间链路整合答案

该策略更偏“多跳链式推理”。

### 4.6 NaiveRAG 策略

- 直接检索 top_k 后汇总答案
- 逻辑简单，适合快速 baseline

---

## 5. 配置体系分析

配置文件：`deepsearcher/config.yaml`

包含三层：
- `provide_settings`：五大能力组件（llm/embedding/file_loader/web_crawler/vector_db）
- `query_settings`：如 `max_iter`
- `load_settings`：如 `chunk_size/chunk_overlap`

默认组合：
- LLM：OpenAI（`o1-mini`）
- Embedding：OpenAIEmbedding（`text-embedding-ada-002`）
- File Loader：PDFLoader
- Web Crawler：FireCrawlCrawler
- Vector DB：Milvus（`uri: ./milvus.db`）

特点：
- provider 参数统一在 YAML 中管理
- 环境变量可覆盖关键密钥
- 模块选择通过 class name 与 `__import__` 动态加载

---

## 6. 接口与使用方式

### 6.1 Python SDK
典型流程：
1. `Configuration()` + `set_provider_config`
2. `init_config`
3. `load_from_local_files` / `load_from_website`
4. `query`

### 6.2 CLI
入口：`deepsearcher/cli.py`
- `deepsearcher query <query> --max_iter 3`
- `deepsearcher load <paths...> --collection_name ...`

支持 URL 与本地路径混合加载。

### 6.3 FastAPI 服务
入口：`main.py`
主要接口：
- `POST /set-provider-config/`
- `POST /load-files/`
- `POST /load-website/`
- `GET /query/`

可选 CORS 开关。

---

## 7. 可扩展性设计

项目通过“统一基类 + provider 导出 + 配置动态实例化”实现扩展：

- LLM：继承 `BaseLLM`，实现 `chat`
- Embedding：继承 `BaseEmbedding`，实现 `embed_query` + `dimension`
- VectorDB：继承 `BaseVectorDB`，实现 collection/search/insert/list
- FileLoader：继承 `BaseLoader`
- WebCrawler：继承 `BaseCrawler`

新增 provider 的典型动作：
1. 新增实现文件
2. 在对应 `__init__.py` 导出类名
3. 在 `config.yaml` 中配置 provider 名与参数

---

## 8. 测试与质量保障

### 8.1 测试组织
`tests/` 按功能分层：
- `tests/agent`
- `tests/embedding`
- `tests/llm`
- `tests/vector_db`
- `tests/loader`
- `tests/utils`

涵盖接口初始化、逻辑分支、mock 行为与部分集成式验证。

### 8.2 代码规范
- Makefile 提供：
  - `make lint` -> `uv run ruff format --diff && uv run ruff check`
  - `make format` -> 自动格式化+修复
- CI `ruff.yml` 在 push/PR 执行 Ruff。

### 8.3 当前环境下的验证状态
本次分析环境缺少 `uv/ruff/pytest` 可执行依赖，无法在本地执行 lint 与测试命令；但仓库中已明确约定对应命令与工作流。

---

## 9. 文档与发布体系

- 文档：`mkdocs.yml` 定义了完整导航，覆盖安装、配置、用法、示例、模块支持、FAQ
- 发布：`.github/workflows/release.yml` 监听 tag `v*`，构建并发布 PyPI
- 文档站：存在 docs 相关工作流（构建/部署）

说明项目在“可用性 + 对外传播”上已经具备较完整工程化基础。

---

## 10. 规模与复杂度粗略画像

基于目录统计：
- `deepsearcher/` 文件数：72
- `tests/` 文件数：65
- `docs/` 文件数：31

结论：
- 代码规模中等
- provider 覆盖广
- 测试与文档投入较高
- 核心复杂度集中在 agent 推理策略与多 provider 兼容

---

## 11. 架构优势与潜在风险

### 11.1 优势
- **高可配置**：通过 YAML + 工厂实现组件替换
- **多策略检索**：Router + DeepSearch/ChainOfRAG 形成策略组合
- **接口友好**：SDK/CLI/API 三栈并行
- **工程完整**：测试、文档、CI、发布链路齐全

### 11.2 潜在风险/关注点
- 使用全局单例（`configuration` 模块变量）时需关注多线程/多请求隔离
- 动态导入依赖 class 名，配置拼写错误会在运行时暴露
- 不同 provider 的异常语义差异较大，统一错误处理可继续加强
- 强依赖外部 API 与密钥管理，需在部署场景下完善运维规范

---

## 12. 后续优化建议（面向仓库演进）

1. 进一步统一 provider 异常映射与错误码输出
2. 在 API 层增加更细粒度参数校验与错误提示
3. 补充端到端 smoke test（最小配置 + 本地 milvus lite）
4. 对全局状态注入方式做可选“上下文化”改造，提升并发安全性
5. 为新增 provider 提供统一模板与 checklist，降低贡献门槛

---

## 13. 总结

`deep-searcher` 是一个以“私有知识检索+多轮推理”为核心的可扩展 RAG 框架。其核心价值在于：
- 使用统一抽象屏蔽多 provider 差异
- 通过不同 agent 策略覆盖“报告型”与“多跳事实型”查询
- 提供完整的开发、测试、文档与发布工程支撑

从仓库现状看，项目已经具备较强的实用性与二次开发基础，适合继续向企业级知识系统方向演进。
